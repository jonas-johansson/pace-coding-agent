#!/bin/bash
set -e
PACE_DIR="$(cd "$(dirname "$0")/.." && pwd)"

if [ ! -f "$PACE_DIR/dist/app.js" ] || [ -n "$(find "$PACE_DIR/src" -name '*.ts' -newer "$PACE_DIR/dist/app.js" -print -quit)" ]; then
  (cd "$PACE_DIR" && npm run build)
fi

node "$PACE_DIR/dist/app.js"
